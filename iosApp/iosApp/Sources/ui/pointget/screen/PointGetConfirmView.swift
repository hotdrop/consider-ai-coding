import SwiftUI

// TODO 未実装
struct PointGetConfirmView: View {
    var body: some View {
        Text("Confirm View")
            .frame(maxWidth: .infinity, maxHeight: .infinity, alignment: .center)
            .padding()
    }
}
