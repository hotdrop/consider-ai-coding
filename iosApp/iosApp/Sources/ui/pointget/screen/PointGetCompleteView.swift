import SwiftUI

// TODO 未実装
struct PointGetCompleteView: View {
    var body: some View {
        Text("Complete View")
            .frame(maxWidth: .infinity, maxHeight: .infinity, alignment: .center)
                        .padding()
    }
}
